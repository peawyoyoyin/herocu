extends common/layout
block content
  h3 Add SSH Key
  p Add your SSH key to the server.
  p This will allow you to use git to push to the server or pull from the server.
  br
  form.form-control(method="POST")
    label(for="content") Copy the content of your public key file here.
    br
    textarea.my-2.form-control(id="content" name="content", cols="100", rows="2")
    br
    button(type="submit").btn.btn-primary Add Key
